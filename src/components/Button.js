import React, { Component } from "react";

class Button extends Component {
  render() {
    const { label, icon, type, onClick, color } = this.props;
    return <div></div>;
  }
}

export default Button;
