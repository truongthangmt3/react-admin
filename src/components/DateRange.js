import React, { Component } from "react";

class DateRange extends Component {
  render() {
    const { from, to, onSelectFrom, onSelectTo, dataFormat } = this.props;

    return <div></div>;
  }
}

export default DateRange;
