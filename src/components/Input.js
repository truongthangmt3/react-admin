import React, { Component } from "react";

class Input extends Component {
  render() {
    const { type, onChange, value, placeholder } = this.props;

    return <div></div>;
  }
}

export default Input;
